node_modules
dist
.env
.env.*
!.env.example
*.log
.DS_Store
.sentryclirc

# Material de trabajo de una sesión de depuración (generadores de archivos hostiles y
# harness offline). No es producto: no se lintea ni se versiona.
scratch/

# Scratch de depuración (no versionado)
scratch/

# Libros hostiles GENERADOS (`bun run hostiles:generar`). Se regeneran a partir de
# `src/lib/hostiles/`, que sí está versionado y es la fuente de verdad — versionar los .xlsx
# metería binarios que se desincronizan del generador sin que nada lo note.
exceles-hostiles/
